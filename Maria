       
<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Maria</title>
    <style>
        /* Corpo da página */
        body {
            font-family: Arial, sans-serif;
            background-color:#cf8d84;
            background-size: cover;
            background-position: center;
            margin: 0;
            padding: 0;
        }

        /* Container do perfil */
        .profile-container {
            display: flex;
            flex-direction: column;
            align-items: center;
            padding: 20px;
            background-color: #6f3730d2; /* Para dar destaque ao conteúdo */
            border-radius: 10px;
            box-shadow: 0 0 15px rgba(0, 0, 0, 0.3);
            width: 100%;
            max-width: 800px;
            margin: 50px auto; /* Adicionado para centralizar */
        }

        /* Foto de perfil */
        .profile-img {
            width: 140px;
            height: 160px;
            border-radius: 25PX;
            border: 2px solid rgb(144, 144, 144);
            margin-top: -60px;
            box-shadow: 0 0 15px rgba(0, 0, 0, 0.3);
        }

        /* Nome e título */
        .profile-info {
            text-align: center;
            color: rgb(0, 0, 0);
        }

        .profile-info h1 {
            margin: 10px 0;
            font-size: 30px;
            font-weight: bold;
        }

        .profile-info p {
            font-size: 18px;
            color: #000000;
            margin: 5px 0;
        }

        /* Descrição e dados adicionais */
        .profile-details {
            background-color:#ffbeb498;
            width: 100%;
            max-width: 700px;
            margin-top: 20px;
            padding: 20px;
            border-radius: 10px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        }

        .profile-details h2 {
            font-size: 24px;
            color: #333;
            margin-bottom: 10px;
        }

        .profile-details p {
            font-size: 50px;
            color: #000000;
            text-align: center;
        }

        .quote {
            margin-top: 20px;
            font-size: 18px;
            color: #000000;
            font-style: italic;
            text-align: center;
        }

        /* Botão de seguir */
        .follow-button {
            background-color: #717171;
            color: rgb(0, 0, 0);
            padding: 10px 20px;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            font-size: 16px;
            margin-top: 20px;
        }

        .follow-button:hover {
            background-color: #000304;
        }
    </style>
</head>
<body>

    <!-- Container do perfil -->
    <div class="profile-container">
        <!-- foto de perfil (1).jpg -->
        <img class="profile-img" src="WhatsApp Image 2025-06-02 at 18.25.29.jpeg" alt="foto de perfil">

        <!-- Informações do perfil -->
       

        <!-- Detalhes adicionais -->
        <div class="profile-details">
           <p>Eu Te Amo!</p>
        </div>

          

        <!-- Frase de destaque -->
        <div class="quote">
            <p>Com amor e carinho!</p>
        </div>

       
    
    
